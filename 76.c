/*76.题目：809*??=800*??+9*?? 
其中??代表的两位数, 809*??为四位数，
8*??的结果为两位数，9*??
的结果为3位数。求??代表的两位数，及809*??后的结果。*/
#include<stdio.h>
int main()
{
	int i;
	for(i=10;i<100;i++)
	   {
	   	if((809*i==800*i+9*i)&&((8*i<100)&&8*i>=10)&&(9*i>=100&&(9*i<1000)))
	   	break;
		}
printf("该数为%d\n809*??后的结果为%d",i,809*i); 
		return 0;
}
